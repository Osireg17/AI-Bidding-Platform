---
phase: 01-service-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.work
  - services/banking-service/go.mod
  - services/banking-service/cmd/banking-service/main.go
  - services/banking-service/internal/config/config.go
autonomous: true
requirements:
  - INF-01
  - INF-04
tags:
  - go
  - scaffolding
  - workspace
  - config

must_haves:
  truths:
    - "Running `grep -F './services/banking-service' go.work` from repo root prints a matching line"
    - "Running `go build ./...` inside services/banking-service exits 0"
    - "Running `go vet ./...` inside services/banking-service exits 0"
    - "Running `go run ./cmd/banking-service` inside services/banking-service exits 0 (stub main returns immediately)"
    - "config.Load() with DATABASE_URL and RABBITMQ_URL set returns Config.Port == 8084 when neither PORT nor BANKING_SERVICE_PORT is set"
  artifacts:
    - path: "services/banking-service/go.mod"
      provides: "Module declaration + direct dependencies pinned to workspace versions"
      contains: "module github.com/Osireg17/AI-Bidding-Platform/services/banking-service"
    - path: "services/banking-service/cmd/banking-service/main.go"
      provides: "Minimal `package main` entry point that satisfies `go build ./...`"
      contains: "func main()"
    - path: "services/banking-service/internal/config/config.go"
      provides: "Config struct + Load() with PORT/BANKING_SERVICE_PORT precedence and required DB+RabbitMQ env vars"
      contains: "BANKING_SERVICE_PORT"
    - path: "go.work"
      provides: "Workspace registration for banking-service module"
      contains: "./services/banking-service"
  key_links:
    - from: "go.work"
      to: "services/banking-service/go.mod"
      via: "workspace use directive"
      pattern: "./services/banking-service"
    - from: "services/banking-service/cmd/banking-service/main.go"
      to: "module github.com/Osireg17/AI-Bidding-Platform/services/banking-service"
      via: "package main resolves under the new module path"
      pattern: "package main"
---

<objective>
Scaffold the banking-service Go module: register it in the workspace, create go.mod with the workspace-pinned direct dependencies, create a minimal `cmd/banking-service/main.go` stub, and implement `internal/config/config.go` with port 8084 defaulting and required env-var validation.

Purpose: Establish a buildable module skeleton so Plan 02 (domain types, ports, errors) can compile against it. This plan delivers INF-04 in full and the foundation of INF-01 (port 8084 surfaced through config; `/health` runtime wiring is deferred to Phase 4 per ROADMAP).

Output: A `services/banking-service/` directory that runs `go build ./...` and `go vet ./...` clean, with `go.work` listing the new module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-service-foundation/01-CONTEXT.md
@.planning/phases/01-service-foundation/01-RESEARCH.md
@.planning/phases/01-service-foundation/01-PATTERNS.md
@claude/claude.local.md

<interfaces>
<!-- Source-of-truth analogs the executor MUST mirror. Extracted from codebase. -->

From go.work (repo root, current contents):
```
go 1.25.6

use (
    ./services/auction-service
    ./services/bff
    ./services/bid-service
    ./services/bot-service
    ./shared
)
```
The `use` block MUST gain exactly one new line: `./services/banking-service`. The `go 1.25.6` directive is unchanged.

From services/bid-service/go.mod (authoritative version source for the workspace):
- Module path style: `module github.com/Osireg17/AI-Bidding-Platform/services/<svc>`
- `go 1.25.6`
- Direct require versions:
  - github.com/gin-gonic/gin v1.12.0
  - github.com/rabbitmq/amqp091-go v1.10.0
  - github.com/stretchr/testify v1.11.1
  - github.com/uptrace/bun v1.2.18
  - github.com/uptrace/bun/dialect/pgdialect v1.2.18
  - github.com/uptrace/bun/driver/pgdriver v1.2.18
  - go.uber.org/zap v1.27.1

From services/auction-service/internal/config/config.go (config Load() pattern — exact analog):
- `package config`
- `type Config struct { Port int; DatabaseURL string; RabbitMQURL string; LogLevel string; ... }`
- `func Load() (*Config, error)` reads env vars: `PORT` (Railway-injected) takes precedence; `<SVC>_SERVICE_PORT` is the local-dev fallback.
- `DATABASE_URL` and `RABBITMQ_URL` are required — return `fmt.Errorf("DATABASE_URL is required")` / `"RABBITMQ_URL is required"` if absent.
- `LOG_LEVEL` defaults to `"info"`.
- Helpers: `getEnv(key, fallback string) string` using `os.LookupEnv`; `getEnvInt(key string, fallback int) int` using `strconv.Atoi`.
- No `init()` side effects.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register banking-service in go.work and create module go.mod</name>
  <files>
    go.work,
    services/banking-service/go.mod
  </files>
  <read_first>
    go.work,
    services/bid-service/go.mod,
    .planning/phases/01-service-foundation/01-PATTERNS.md
  </read_first>
  <action>
    Append the line `    ./services/banking-service` to the `use ( ... )` block in `go.work`. Preserve existing entries and the `go 1.25.6` directive. Do not reorder existing lines.

    Create `services/banking-service/go.mod` declaring `module github.com/Osireg17/AI-Bidding-Platform/services/banking-service` and `go 1.25.6`. Add a `require ( ... )` block with exactly these direct dependencies, versions matching bid-service/go.mod verbatim: `github.com/gin-gonic/gin v1.12.0`, `github.com/rabbitmq/amqp091-go v1.10.0`, `github.com/stretchr/testify v1.11.1`, `github.com/uptrace/bun v1.2.18`, `github.com/uptrace/bun/dialect/pgdialect v1.2.18`, `github.com/uptrace/bun/driver/pgdriver v1.2.18`, `go.uber.org/zap v1.27.1`. Do not invent versions. Leave the indirect block empty for now — Task 3 runs `go mod tidy` after all source files exist, which populates it from the workspace cache.

    Do not create `internal/repo/`, `internal/service/`, `internal/http/`, or `internal/mq/` directories yet — per RESEARCH.md Recommended Project Structure, empty dirs are added by the phase that uses them.
  </action>
  <verify>
    <automated>grep -F './services/banking-service' /Users/osiobomighie/Documents/Developer/AI-Bidding-Platform/go.work &amp;&amp; grep -F 'module github.com/Osireg17/AI-Bidding-Platform/services/banking-service' /Users/osiobomighie/Documents/Developer/AI-Bidding-Platform/services/banking-service/go.mod &amp;&amp; grep -F 'github.com/uptrace/bun v1.2.18' /Users/osiobomighie/Documents/Developer/AI-Bidding-Platform/services/banking-service/go.mod</automated>
  </verify>
  <acceptance_criteria>
    - `go.work` line `./services/banking-service` is present inside the `use ( ... )` block (not outside it).
    - `go.work` preserves all 5 pre-existing `use` entries (auction-service, bff, bid-service, bot-service, shared).
    - `services/banking-service/go.mod` first non-blank line equals exactly `module github.com/Osireg17/AI-Bidding-Platform/services/banking-service`.
    - `services/banking-service/go.mod` contains `go 1.25.6`.
    - `services/banking-service/go.mod` `require` block lists exactly these 7 direct dependencies with versions matching bid-service/go.mod verbatim (gin v1.12.0, amqp091-go v1.10.0, testify v1.11.1, bun v1.2.18, pgdialect v1.2.18, pgdriver v1.2.18, zap v1.27.1).
    - No additional services directories (repo/service/http/mq) exist under `services/banking-service/internal/` after this task.
  </acceptance_criteria>
  <done>
    `go.work` lists banking-service; `services/banking-service/go.mod` exists with the correct module path, Go toolchain version, and the 7 direct dependencies pinned to workspace versions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create main.go stub and config.go with port 8084 default</name>
  <files>
    services/banking-service/cmd/banking-service/main.go,
    services/banking-service/internal/config/config.go
  </files>
  <read_first>
    services/auction-service/internal/config/config.go,
    services/banking-service/go.mod,
    .planning/phases/01-service-foundation/01-PATTERNS.md
  </read_first>
  <action>
    Create `services/banking-service/cmd/banking-service/main.go` as a minimal stub: `package main` declaration and `func main() {}` with an empty body. No imports, no Gin router, no DB connection, no logger. Phase 4 overwrites this file with the full wiring per ROADMAP.md Pitfall 5 (Phase 1 must not boot infrastructure).

    Create `services/banking-service/internal/config/config.go` modeled exactly on `services/auction-service/internal/config/config.go`. `package config`. Define a `Config` struct with fields `Port int`, `DatabaseURL string`, `RabbitMQURL string`, `LogLevel string` — only these four fields (no `BidServiceURL`, no `GeminiAPIKey`, no `time.Duration` fields; banking-service has no scheduled jobs in this milestone).

    Implement `func Load() (*Config, error)` with this precedence: read `PORT` via `getEnvInt("PORT", 0)`; if 0, fall back to `getEnvInt("BANKING_SERVICE_PORT", 8084)`. The literal `8084` is the locked banking-service port from STATE.md "Active Constraints". Read `DATABASE_URL` via `getEnv` with empty fallback; if empty, return `fmt.Errorf("DATABASE_URL is required")`. Read `RABBITMQ_URL` via `getEnv` with empty fallback; if empty, return `fmt.Errorf("RABBITMQ_URL is required")`. Read `LOG_LEVEL` via `getEnv("LOG_LEVEL", "info")`. Return a populated `*Config`.

    Define `getEnv(key, fallback string) string` using `os.LookupEnv` and `getEnvInt(key string, fallback int) int` using `os.LookupEnv` + `strconv.Atoi` — copy these two helper functions verbatim from `services/auction-service/internal/config/config.go`. Imports: `fmt`, `os`, `strconv`. Do not import `time` (unused). Do not declare any `init()` function.
  </action>
  <verify>
    <automated>cd /Users/osiobomighie/Documents/Developer/AI-Bidding-Platform/services/banking-service &amp;&amp; gofmt -l cmd internal | (! grep .) &amp;&amp; grep -F 'package main' cmd/banking-service/main.go &amp;&amp; grep -F 'BANKING_SERVICE_PORT' internal/config/config.go &amp;&amp; grep -F '8084' internal/config/config.go &amp;&amp; grep -F 'DATABASE_URL is required' internal/config/config.go</automated>
  </verify>
  <acceptance_criteria>
    - `services/banking-service/cmd/banking-service/main.go` contains exactly `package main` and a `func main()` declaration with an empty body. No imports, no other top-level declarations.
    - `services/banking-service/internal/config/config.go` declares `package config`.
    - `Config` struct has fields `Port int`, `DatabaseURL string`, `RabbitMQURL string`, `LogLevel string` and no other fields.
    - `Load()` returns a non-nil error containing the substring `DATABASE_URL is required` when `DATABASE_URL` is unset.
    - `Load()` returns a non-nil error containing the substring `RABBITMQ_URL is required` when `DATABASE_URL` is set but `RABBITMQ_URL` is unset.
    - When `DATABASE_URL=x` and `RABBITMQ_URL=x` are set but `PORT` and `BANKING_SERVICE_PORT` are unset, `Load()` returns `Config.Port == 8084`.
    - `gofmt -l` reports no diff for both files.
    - Neither file declares an `init()` function.
  </acceptance_criteria>
  <done>
    `cmd/banking-service/main.go` is a stub with `package main` and empty `func main()`; `internal/config/config.go` exposes `Config{Port, DatabaseURL, RabbitMQURL, LogLevel}` and `Load()` defaulting Port to 8084 and requiring DATABASE_URL + RABBITMQ_URL.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run `go mod tidy` and confirm module builds clean</name>
  <files>
    services/banking-service/go.mod,
    services/banking-service/go.sum
  </files>
  <read_first>
    services/banking-service/go.mod,
    services/banking-service/cmd/banking-service/main.go,
    services/banking-service/internal/config/config.go
  </read_first>
  <action>
    From `services/banking-service/`, run `go mod tidy` to populate the indirect dependency block and generate `go.sum`. Resolve modules from the workspace cache — versions of indirect deps come from sibling services in go.work, so no new versions are introduced.

    After `go mod tidy`, run `go build ./...` and `go vet ./...` from `services/banking-service/`. Both must exit 0. The build will succeed even though `gin`, `bun`, `amqp091-go`, `testify`, and `zap` are listed in `require` but unused yet — Go treats unused declared deps as a `go mod tidy` concern, not a build error; however, if `go mod tidy` prunes them, that is acceptable because Plan 02 and later phases will re-add them via real imports. Do not manually re-add anything `go mod tidy` removes.

    If `go mod tidy` removes any of the 7 direct deps because no file imports them yet, that is expected and acceptable for Phase 1 scope — Phase 2 onward will re-introduce them as their imports appear. Do not fight the tidy result.

    Do not modify `internal/config/config.go` or `cmd/banking-service/main.go` in this task — those are owned by Task 2.
  </action>
  <verify>
    <automated>cd /Users/osiobomighie/Documents/Developer/AI-Bidding-Platform/services/banking-service &amp;&amp; go build ./... &amp;&amp; go vet ./... &amp;&amp; go run ./cmd/banking-service</automated>
  </verify>
  <acceptance_criteria>
    - `cd services/banking-service &amp;&amp; go build ./...` exits 0 with no stderr output.
    - `cd services/banking-service &amp;&amp; go vet ./...` exits 0 with no stderr output.
    - `cd services/banking-service &amp;&amp; go run ./cmd/banking-service` exits 0 (stub `main()` returns immediately).
    - `services/banking-service/go.sum` exists.
    - `grep -F './services/banking-service' go.work` from repo root prints one matching line.
  </acceptance_criteria>
  <done>
    Module builds and vets clean; `go.sum` is generated; the workspace resolves banking-service from the repo root.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → environment | `config.Load()` reads `DATABASE_URL`, `RABBITMQ_URL`, `PORT`, `BANKING_SERVICE_PORT`, `LOG_LEVEL` from the OS environment. No other untrusted input crosses any boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `config.Load()` error messages | accept | Error strings name the missing env var (`DATABASE_URL is required`) but never echo a value. No secrets logged in this plan. |
| T-01-02 | Tampering | dependency versions in `go.mod` | mitigate | Direct deps are pinned to workspace-verified versions (see `<interfaces>`). `go.sum` records cryptographic hashes; subsequent `go build` enforces them. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — all dependencies are already present in the workspace via existing services (per RESEARCH.md Package Legitimacy Audit). `go mod tidy` resolves from the workspace cache, not from the public proxy fresh. |
</threat_model>

<verification>
- `grep -F './services/banking-service' go.work` prints a matching line
- `cd services/banking-service && go build ./...` exits 0
- `cd services/banking-service && go vet ./...` exits 0
- `cd services/banking-service && go run ./cmd/banking-service` exits 0
- `gofmt -l services/banking-service/cmd services/banking-service/internal` prints nothing
- All three tasks’ acceptance criteria satisfied
</verification>

<success_criteria>
- Workspace recognises the new module (`grep "banking-service" go.work` succeeds — ROADMAP Phase 1 success criterion 3)
- `go build ./...` clean (ROADMAP Phase 1 success criterion 1)
- `go vet ./...` clean (ROADMAP Phase 1 success criterion 2)
- `config.Load()` surfaces port 8084 by default and rejects missing DATABASE_URL / RABBITMQ_URL
- Plan 02 can add files under `services/banking-service/internal/domain/` and have them compile within this module
</success_criteria>

<output>
Create `.planning/phases/01-service-foundation/01-01-SUMMARY.md` when done.
</output>
