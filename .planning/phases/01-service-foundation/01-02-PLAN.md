---
phase: 01-service-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - services/banking-service/internal/domain/wallet.go
  - services/banking-service/internal/domain/item.go
  - services/banking-service/internal/domain/errors.go
  - services/banking-service/internal/domain/errors_test.go
  - services/banking-service/internal/domain/ports.go
  - services/banking-service/go.mod
  - services/banking-service/go.sum
autonomous: true
requirements:
  - INF-01
tags:
  - go
  - domain
  - ports-and-adapters
  - bun

must_haves:
  truths:
    - "Wallet struct exists in package domain with BotID as primary key and Balance as float64 per D-01/D-02/D-03"
    - "Item struct exists in package domain with auto-increment ID, CreatedAt only (no UpdatedAt) per D-04/D-05/D-06"
    - "Sentinel errors ErrWalletNotFound, ErrItemNotFound, ErrInsufficientBalance, ErrItemNotOwnedByBot are exported and distinct"
    - "WalletRepository, ItemRepository, BankingService, EventConsumer interfaces are defined in domain package"
    - "D-07: WalletRepository.GetWallet and ItemRepository.GetItemsByBot are defined as separate methods — no combined wallet+items method exists in any port interface, enforcing service-layer assembly over bun has-many"
    - "Bot ID constants BotAlice=1, BotSteve=2, BotVictor=3, BotCharlie=4 are exported int64 constants in domain package"
    - "`go build ./...` and `go vet ./...` exit 0 inside services/banking-service"
    - "`go test ./internal/domain/... -run TestSentinelErrors` exits 0 and passes"
  artifacts:
    - path: "services/banking-service/internal/domain/wallet.go"
      provides: "Wallet bun model + bot ID constants"
      contains: "type Wallet struct"
    - path: "services/banking-service/internal/domain/item.go"
      provides: "Item bun model"
      contains: "type Item struct"
    - path: "services/banking-service/internal/domain/errors.go"
      provides: "Four sentinel errors referenced by all downstream phases"
      contains: "ErrInsufficientBalance"
    - path: "services/banking-service/internal/domain/errors_test.go"
      provides: "Distinctness test for the four sentinels using errors.Is"
      contains: "TestSentinelErrors"
    - path: "services/banking-service/internal/domain/ports.go"
      provides: "WalletRepository, ItemRepository, BankingService, EventConsumer port interfaces (withTx pattern)"
      contains: "WithTx"
  key_links:
    - from: "services/banking-service/internal/domain/ports.go"
      to: "services/banking-service/internal/domain/wallet.go"
      via: "interface methods return *Wallet"
      pattern: "\\*Wallet"
    - from: "services/banking-service/internal/domain/ports.go"
      to: "services/banking-service/internal/domain/item.go"
      via: "interface methods return *Item"
      pattern: "\\*Item"
    - from: "services/banking-service/internal/domain/errors_test.go"
      to: "services/banking-service/internal/domain/errors.go"
      via: "errors.Is assertions on exported sentinels"
      pattern: "errors\\.Is"
---

<objective>
Define the domain layer for banking-service: Wallet and Item bun models, the four sentinel errors named in ROADMAP Phase 1 success criterion 5, the port interfaces (WalletRepository, ItemRepository, BankingService, EventConsumer) with the withTx pattern, and bot ID constants. Add a distinctness test for the sentinels.

Purpose: These contracts are the foundation every subsequent phase implements — Phase 2 repo will satisfy `WalletRepository` and `ItemRepository`; Phase 3 service will satisfy `BankingService`; Phase 4 consumer will satisfy `EventConsumer`. The withTx pattern (new to this codebase) is locked in here so all downstream implementations agree on the transaction-scoping shape.

Output: Five Go files under `services/banking-service/internal/domain/` that compile clean and pass a distinctness test for the four sentinels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-service-foundation/01-CONTEXT.md
@.planning/phases/01-service-foundation/01-RESEARCH.md
@.planning/phases/01-service-foundation/01-PATTERNS.md
@.planning/phases/01-service-foundation/01-01-SUMMARY.md
@claude/claude.local.md

<interfaces>
<!-- Source-of-truth analogs the executor MUST mirror. Extracted from codebase + locked decisions. -->

From services/bid-service/internal/domain/bid.go (bun struct tag style — copy verbatim):
- Embed `bun.BaseModel` with `bun:"table:<name>"` tag
- Natural PK: ``bun:",pk"`` (no autoincrement)
- Auto-increment PK: ``bun:",pk,autoincrement"``
- Required column: ``bun:",notnull"``
- Timestamp with DB default: ``bun:",nullzero,notnull,default:current_timestamp"``
- Imports: `"time"` and `"github.com/uptrace/bun"`

From services/bid-service/internal/domain/errors.go (sentinel style — exact analog):
```
package domain

import "errors"

var (
    ErrXxx = errors.New("descriptive lowercase message")
)
```

From services/bid-service/internal/domain/ports.go (interface style — exact analog):
- `package domain`
- Imports: `"context"` (and `"time"` if any method uses durations)
- All methods take `ctx context.Context` as the first parameter
- Methods grouped by domain entity; one interface per concern
- Return errors directly — no wrapping at the interface boundary
- `Close() error` on long-lived consumer/publisher interfaces

Locked CONTEXT.md decisions D-01..D-07 (all 7 are non-negotiable):
- D-01: Wallet embeds bun.BaseModel + has CreatedAt and UpdatedAt
- D-02: Wallet PK is `BotID int64 bun:",pk"` — no autoincrement
- D-03: Wallet table name: `wallets` (no alias)
- D-04: Item has `ID int64 bun:",pk,autoincrement"`
- D-05: Item has `CreatedAt time.Time` only — no UpdatedAt
- D-06: Item table name: `items` (no alias)
- D-07: Service layer assembles wallet + items via two separate repo calls — port interfaces MUST expose `GetWallet` and `GetItemsByBot` as separate methods, NOT a bun has-many relation

Locked STATE.md constraints:
- Bot IDs are pre-known constants 1–4 (Alice, Steve, Victor, Charlie) — replicate locally; bot-service's `internal/` cannot be imported across modules
- `withTx(tx bun.Tx)` pattern on repos — Phase 1 chooses the interface shape; Plans 2 onward must satisfy whatever is locked here
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create wallet.go (Wallet model + bot ID constants) and item.go (Item model)</name>
  <files>
    services/banking-service/internal/domain/wallet.go,
    services/banking-service/internal/domain/item.go
  </files>
  <read_first>
    services/bid-service/internal/domain/bid.go,
    services/bot-service/internal/domain/bot.go,
    .planning/phases/01-service-foundation/01-CONTEXT.md,
    .planning/phases/01-service-foundation/01-PATTERNS.md
  </read_first>
  <behavior>
    - Wallet struct: BotID is the primary key (natural, not autoincrement); Balance is float64 notnull; CreatedAt and UpdatedAt are time.Time with the standard `nullzero,notnull,default:current_timestamp` tag.
    - Item struct: ID is autoincrement primary key; BotID, AuctionID, Title, PurchasePrice are notnull; CreatedAt only (no UpdatedAt).
    - Bot ID constants: BotAlice=1, BotSteve=2, BotVictor=3, BotCharlie=4 declared as `const` `int64` — referenceable by Phase 2 seed code.
    - `go build ./internal/domain/...` exits 0 from `services/banking-service/`.
    - `go vet ./internal/domain/...` exits 0.
  </behavior>
  <action>
    Create `services/banking-service/internal/domain/wallet.go`. Declare `package domain`. Import `"time"` and `"github.com/uptrace/bun"`. Define `type Wallet struct` with embedded `bun.BaseModel` tagged ``bun:"table:wallets"`` (no alias per D-03), fields in this order: `BotID int64` tagged ``bun:",pk"`` (D-02), `Balance float64` tagged ``bun:",notnull"``, `CreatedAt time.Time` tagged ``bun:",nullzero,notnull,default:current_timestamp"`` (D-01), `UpdatedAt time.Time` tagged ``bun:",nullzero,notnull,default:current_timestamp"`` (D-01). No constructor function — Wallet is a pure data struct in Phase 1 (validation lives in the service layer in Phase 3).

    In the same file, declare four exported `int64` constants in a `const ( ... )` block: `BotAlice int64 = 1`, `BotSteve int64 = 2`, `BotVictor int64 = 3`, `BotCharlie int64 = 4`. These mirror the bot identities from `services/bot-service/internal/domain/bot.go`; replicate locally because `internal/` packages cannot be imported across modules (CONTEXT.md code_context note).

    Create `services/banking-service/internal/domain/item.go`. Declare `package domain`. Import `"time"` and `"github.com/uptrace/bun"`. Define `type Item struct` with embedded `bun.BaseModel` tagged ``bun:"table:items"`` (D-06), fields in this order: `ID int64` tagged ``bun:",pk,autoincrement"`` (D-04), `BotID int64` tagged ``bun:",notnull"``, `AuctionID int64` tagged ``bun:",notnull"``, `Title string` tagged ``bun:",notnull"``, `PurchasePrice float64` tagged ``bun:",notnull"``, `CreatedAt time.Time` tagged ``bun:",nullzero,notnull,default:current_timestamp"`` (D-05). Do not add `UpdatedAt` — D-05 explicitly forbids it (items are immutable: insert on win, delete on buyout).

    Both files must be `gofmt`-clean. No `init()` functions. No package-level vars besides the const block in `wallet.go`. Do not introduce business logic (validation, comparisons, formatters) — domain types in Phase 1 are pure data per CONTEXT.md decisions and §4 of claude.local.md.
  </action>
  <verify>
    <automated>cd /Users/osiobomighie/Documents/Developer/AI-Bidding-Platform/services/banking-service &amp;&amp; go build ./internal/domain/... &amp;&amp; go vet ./internal/domain/... &amp;&amp; gofmt -l internal/domain | (! grep .) &amp;&amp; grep -F 'bun:"table:wallets"' internal/domain/wallet.go &amp;&amp; grep -F 'bun:",pk,autoincrement"' internal/domain/item.go &amp;&amp; grep -F 'BotCharlie int64 = 4' internal/domain/wallet.go</automated>
  </verify>
  <acceptance_criteria>
    - `services/banking-service/internal/domain/wallet.go` declares `package domain` and contains `type Wallet struct` with exactly these fields in order: embedded `bun.BaseModel`, `BotID int64`, `Balance float64`, `CreatedAt time.Time`, `UpdatedAt time.Time`.
    - Wallet BaseModel tag is exactly ``bun:"table:wallets"`` (no `,alias:` segment).
    - Wallet `BotID` tag is exactly ``bun:",pk"`` — must NOT contain `autoincrement`.
    - Wallet `Balance` tag is exactly ``bun:",notnull"``.
    - Wallet `CreatedAt` and `UpdatedAt` tags are exactly ``bun:",nullzero,notnull,default:current_timestamp"``.
    - `wallet.go` contains a `const ( ... )` block with `BotAlice int64 = 1`, `BotSteve int64 = 2`, `BotVictor int64 = 3`, `BotCharlie int64 = 4` (typed `int64`).
    - `services/banking-service/internal/domain/item.go` declares `package domain` and contains `type Item struct` with exactly these fields in order: embedded `bun.BaseModel`, `ID int64`, `BotID int64`, `AuctionID int64`, `Title string`, `PurchasePrice float64`, `CreatedAt time.Time`.
    - Item BaseModel tag is exactly ``bun:"table:items"``.
    - Item `ID` tag is exactly ``bun:",pk,autoincrement"``.
    - `item.go` does NOT declare a field named `UpdatedAt` (grep proof: `grep -c '^\s*UpdatedAt' services/banking-service/internal/domain/item.go` equals 0; the heredoc-style grep is fine here because `item.go` has no comments mentioning UpdatedAt).
    - Both files pass `gofmt -l` (no diff output).
    - `cd services/banking-service &amp;&amp; go build ./internal/domain/...` exits 0.
    - `cd services/banking-service &amp;&amp; go vet ./internal/domain/...` exits 0.
    - Neither file declares an `init()` function or any method on `Wallet`/`Item`.
  </acceptance_criteria>
  <done>
    `wallet.go` defines `Wallet` per D-01/D-02/D-03 plus four bot ID int64 constants; `item.go` defines `Item` per D-04/D-05/D-06; both compile and vet clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create errors.go (4 sentinels) and errors_test.go (distinctness test)</name>
  <files>
    services/banking-service/internal/domain/errors.go,
    services/banking-service/internal/domain/errors_test.go
  </files>
  <read_first>
    services/bid-service/internal/domain/errors.go,
    .planning/ROADMAP.md,
    .planning/phases/01-service-foundation/01-PATTERNS.md
  </read_first>
  <behavior>
    - `errors.go` exports four package-level `var` sentinels created with `errors.New`: `ErrWalletNotFound`, `ErrItemNotFound`, `ErrInsufficientBalance`, `ErrItemNotOwnedByBot` (exact names — ROADMAP Phase 1 success criterion 5).
    - Each sentinel has a distinct lowercase message string.
    - `TestSentinelErrors` in `errors_test.go` asserts that each sentinel matches itself via `errors.Is` and does NOT match any of the other three.
    - `go test ./internal/domain/... -run TestSentinelErrors` exits 0 and the test passes.
  </behavior>
  <action>
    Create `services/banking-service/internal/domain/errors.go`. Declare `package domain`. Import `"errors"`. Declare exactly four exported sentinel variables in a single `var ( ... )` block using `errors.New`:
    - `ErrWalletNotFound = errors.New("wallet not found")`
    - `ErrItemNotFound = errors.New("item not found")`
    - `ErrInsufficientBalance = errors.New("insufficient balance")`
    - `ErrItemNotOwnedByBot = errors.New("item not owned by this bot")`

    Names and message strings are verbatim from RESEARCH.md "Sentinel errors (errors.go)" code example and align with ROADMAP Phase 1 success criterion 5. Do not add extra sentinels in this phase (other domain errors will appear in later phases as their use cases arise).

    Create `services/banking-service/internal/domain/errors_test.go`. Declare `package domain` (same package — these are package-internal tests of variables, not behavior). Import `"errors"`, `"testing"`, and `"github.com/stretchr/testify/assert"`. Define `func TestSentinelErrors(t *testing.T)`. Build a slice of `error` containing the four sentinels in a stable order. For each sentinel, assert `errors.Is(sentinel, sentinel)` is true (self-match). For each pair of distinct sentinels (i, j where i != j), assert `errors.Is(sentinel_i, sentinel_j)` is false (no cross-match). This is the distinctness contract Phase 3 handlers will rely on for HTTP status code mapping.

    Both files must be `gofmt`-clean. No `init()`. No helper functions outside the test.
  </action>
  <verify>
    <automated>cd /Users/osiobomighie/Documents/Developer/AI-Bidding-Platform/services/banking-service &amp;&amp; go test ./internal/domain/... -run TestSentinelErrors -v &amp;&amp; gofmt -l internal/domain/errors.go internal/domain/errors_test.go | (! grep .)</automated>
  </verify>
  <acceptance_criteria>
    - `services/banking-service/internal/domain/errors.go` declares `package domain` and contains a single `var ( ... )` block with exactly four entries, named `ErrWalletNotFound`, `ErrItemNotFound`, `ErrInsufficientBalance`, `ErrItemNotOwnedByBot` — no other exported variables.
    - Each sentinel is initialised via `errors.New("<message>")` with the messages exactly: "wallet not found", "item not found", "insufficient balance", "item not owned by this bot".
    - `services/banking-service/internal/domain/errors_test.go` declares `package domain` and defines `func TestSentinelErrors(t *testing.T)`.
    - `TestSentinelErrors` asserts self-match (errors.Is(e, e) is true) for all four sentinels.
    - `TestSentinelErrors` asserts cross-distinctness (errors.Is(e_i, e_j) is false) for all 12 ordered pairs where i != j.
    - `cd services/banking-service &amp;&amp; go test ./internal/domain/... -run TestSentinelErrors` exits 0 with PASS.
    - Both files pass `gofmt -l` (no diff output).
    - `errors.go` does not declare any `init()` function or non-sentinel exported identifiers.
  </acceptance_criteria>
  <done>
    Four sentinels exist with the exact names from ROADMAP success criterion 5, and a passing test proves they are distinct from one another and self-matching under `errors.Is`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ports.go (repo + service + consumer interfaces, withTx pattern) and run full build</name>
  <files>
    services/banking-service/internal/domain/ports.go,
    services/banking-service/go.mod,
    services/banking-service/go.sum
  </files>
  <read_first>
    services/bid-service/internal/domain/ports.go,
    services/banking-service/internal/domain/wallet.go,
    services/banking-service/internal/domain/item.go,
    .planning/phases/01-service-foundation/01-RESEARCH.md,
    .planning/phases/01-service-foundation/01-PATTERNS.md
  </read_first>
  <behavior>
    - `ports.go` declares `WalletRepository`, `ItemRepository`, `BankingService`, and `EventConsumer` interfaces in package `domain`.
    - `WalletRepository` and `ItemRepository` each expose a `WithTx(tx bun.IDB) <Self>` method that returns a transaction-scoped copy of the same interface — the withTx pattern (RESEARCH Option A).
    - All repo and service methods take `ctx context.Context` as the first parameter.
    - `BankingService.GetWallet` returns `(*Wallet, []*Item, error)` per D-07 (two repo calls assembled at the service layer, not bun has-many).
    - `EventConsumer` exposes `Start(ctx context.Context) error` and `Close() error`.
    - `go build ./...` and `go vet ./...` exit 0 from `services/banking-service/`.
    - The full domain test suite passes: `go test ./internal/domain/...` exits 0.
  </behavior>
  <action>
    First, verify `bun.IDB` exists in v1.2.18 by running `cd services/banking-service && go doc github.com/uptrace/bun.IDB` (pitfall 4 from RESEARCH). If the symbol exists, proceed with Option A (`WithTx(tx bun.IDB) WalletRepository`). If `go doc` reports no symbol, fall back to `bun.Tx` and update both `WithTx` signatures consistently. Record the chosen type in the Task SUMMARY.

    Create `services/banking-service/internal/domain/ports.go`. Declare `package domain`. Imports: `"context"` and `"github.com/uptrace/bun"`. Define four interfaces:

    `WalletRepository` with methods (all taking `ctx context.Context` first):
    - `GetWallet(ctx context.Context, botID int64) (*Wallet, error)`
    - `UpdateBalance(ctx context.Context, botID int64, newBalance float64) error`
    - `InsertWallet(ctx context.Context, wallet *Wallet) error` — needed by Phase 2 seed (idempotent ON CONFLICT DO NOTHING insert)
    - `WithTx(tx bun.IDB) WalletRepository` — returns a transaction-scoped copy

    `ItemRepository` with methods:
    - `GetItem(ctx context.Context, itemID int64) (*Item, error)`
    - `GetItemsByBot(ctx context.Context, botID int64) ([]*Item, error)` — D-07 separate query
    - `InsertItem(ctx context.Context, item *Item) error`
    - `DeleteItem(ctx context.Context, itemID int64) error`
    - `WithTx(tx bun.IDB) ItemRepository`

    `BankingService` with methods (the use cases driving the API in Phase 3):
    - `GetWallet(ctx context.Context, botID int64) (*Wallet, []*Item, error)` — D-07 returns wallet and item list assembled at the service layer
    - `RecordWin(ctx context.Context, botID, auctionID int64, title string, purchasePrice float64) error`
    - `Buyout(ctx context.Context, itemID int64) error`

    `EventConsumer` with methods (matches `EventPublisher.Close()` style from bid-service ports):
    - `Start(ctx context.Context) error`
    - `Close() error`

    Comment each interface block with a one-line purpose describing which phase implements it (Phase 2 for repos, Phase 3 for service, Phase 4 for consumer). Do not embed implementation hints. Do not declare structs or constructors here — `domain/ports.go` is interface-only per claude.local.md §5.

    After writing `ports.go`, run `go mod tidy` from `services/banking-service/` to materialise the bun import into `go.sum`. Then run `go build ./...`, `go vet ./...`, and `go test ./internal/domain/...` — all must exit 0.
  </action>
  <verify>
    <automated>cd /Users/osiobomighie/Documents/Developer/AI-Bidding-Platform/services/banking-service &amp;&amp; go mod tidy &amp;&amp; go build ./... &amp;&amp; go vet ./... &amp;&amp; go test ./internal/domain/... &amp;&amp; gofmt -l internal/domain/ports.go | (! grep .) &amp;&amp; grep -E 'WithTx\(tx (bun\.IDB|bun\.Tx)\) WalletRepository' internal/domain/ports.go &amp;&amp; grep -F 'GetWallet(ctx context.Context, botID int64) (*Wallet, []*Item, error)' internal/domain/ports.go</automated>
  </verify>
  <acceptance_criteria>
    - `services/banking-service/internal/domain/ports.go` declares `package domain` and imports exactly `"context"` and `"github.com/uptrace/bun"` — no other imports.
    - Exactly four interface types are declared in this order: `WalletRepository`, `ItemRepository`, `BankingService`, `EventConsumer` — no other type declarations in the file.
    - `WalletRepository` methods are exactly: `GetWallet(ctx context.Context, botID int64) (*Wallet, error)`, `UpdateBalance(ctx context.Context, botID int64, newBalance float64) error`, `InsertWallet(ctx context.Context, wallet *Wallet) error`, `WithTx(tx bun.IDB) WalletRepository` (or `tx bun.Tx` if `go doc github.com/uptrace/bun.IDB` reports no such symbol).
    - `ItemRepository` methods are exactly: `GetItem(ctx context.Context, itemID int64) (*Item, error)`, `GetItemsByBot(ctx context.Context, botID int64) ([]*Item, error)`, `InsertItem(ctx context.Context, item *Item) error`, `DeleteItem(ctx context.Context, itemID int64) error`, `WithTx(...) ItemRepository`.
    - `BankingService` methods are exactly: `GetWallet(ctx context.Context, botID int64) (*Wallet, []*Item, error)`, `RecordWin(ctx context.Context, botID, auctionID int64, title string, purchasePrice float64) error`, `Buyout(ctx context.Context, itemID int64) error`.
    - `EventConsumer` methods are exactly: `Start(ctx context.Context) error`, `Close() error`.
    - `ports.go` declares no struct, no constructor, no `init()`, no package-level var.
    - `WithTx` method signature on both repo interfaces uses the same tx parameter type (both `bun.IDB` or both `bun.Tx`).
    - `cd services/banking-service && go mod tidy` exits 0; `go.sum` contains entries for `github.com/uptrace/bun`.
    - `cd services/banking-service && go build ./...` exits 0.
    - `cd services/banking-service && go vet ./...` exits 0.
    - `cd services/banking-service && go test ./internal/domain/...` exits 0 and the existing `TestSentinelErrors` PASSes.
    - `gofmt -l services/banking-service/internal/domain/ports.go` prints nothing.
  </acceptance_criteria>
  <done>
    Four interfaces defined with the withTx pattern locked in (bun.IDB if available, bun.Tx otherwise); domain package compiles, vets, and tests clean; go.sum carries bun.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Phase 1 domain layer contains no HTTP handlers, no DB calls, no MQ consumers, no user input. Domain types and port interfaces are pure data + contracts with zero security surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | bun struct tags drive DB schema | mitigate | Tags are pinned verbatim to CONTEXT.md D-01..D-06 and verified by grep in acceptance criteria. Schema generation happens in Phase 2 from these tags — incorrect tags here would propagate. |
| T-01-04 | Repudiation | Item has CreatedAt but no UpdatedAt by design (D-05) | accept | Items are immutable: insert on win, delete on buyout. No update audit trail needed; the win record is the audit event in Phase 4 logs. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; `go mod tidy` resolves `github.com/uptrace/bun` from the workspace cache populated by sibling services (RESEARCH.md Package Legitimacy Audit). |
</threat_model>

<verification>
- `cd services/banking-service && go build ./...` exits 0 (ROADMAP success criterion 1)
- `cd services/banking-service && go vet ./...` exits 0 (ROADMAP success criterion 2)
- `cd services/banking-service && go build ./internal/domain/...` exits 0 (ROADMAP success criterion 4)
- `cd services/banking-service && go test ./internal/domain/...` exits 0 with `TestSentinelErrors` PASS
- `grep -E '^\s*Err(WalletNotFound|ItemNotFound|InsufficientBalance|ItemNotOwnedByBot)\b' services/banking-service/internal/domain/errors.go` prints all four sentinel names (ROADMAP success criterion 5)
- All locked decisions D-01..D-07 are reflected in code per acceptance criteria
</verification>

<success_criteria>
- Wallet and Item bun models exist with exact tag layout from D-01..D-06
- Bot ID constants (1..4) declared in `wallet.go` as exported `int64`
- Four sentinel errors named exactly as in ROADMAP success criterion 5
- `TestSentinelErrors` passes, proving distinctness for downstream `errors.Is` handler mapping
- Four interfaces (`WalletRepository`, `ItemRepository`, `BankingService`, `EventConsumer`) defined with `WithTx` pattern locked in
- Module compiles, vets, and tests clean from the service root
- Plan 02 leaves the workspace in a state where Phase 2 can add `internal/repo/` files that implement `WalletRepository` and `ItemRepository` against the locked port shapes
</success_criteria>

<output>
Create `.planning/phases/01-service-foundation/01-02-SUMMARY.md` when done.
</output>
